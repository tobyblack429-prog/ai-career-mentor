# 本地敏感信息检查与脱敏记录（2026-09-25）

## 范围与处理原则

- 检查当前 Git 跟踪文件、可读取的 7 个本地提交，以及本机被 Git 忽略的项目配置、数据库和日志。按常见 API Key、访问令牌、私钥、带密码的连接串和硬编码身份信息特征筛查；没有调用第三方泄露检测服务。
- 本记录**不保存任何真实密钥、密码、令牌或私人邮箱原文**。
- 按用户选择保留本机运行文件和演示数据；仅清理可能进入代码仓库的内容。

## 检查结果

| 类别 | 结果 | 处理 |
| --- | --- | --- |
| 本机模型配置 | `backend/.env` 含模型密钥等私有配置，已被 Git 忽略且未见于本地 Git 历史 | 保留原文件，不复制进归档 |
| 本机数据与日志 | 本地数据库含简历、面试等演示记录；日志含用户标识和邮箱类信息，均被 Git 忽略 | 保留原文件；提醒不要手工打包上传 |
| 已跟踪文件与历史 | 未发现可确认的真实服务商 API Key、私钥或高置信度访问令牌 | 无需在当前文件中移除真实密钥 |
| 示例连接串 | 文档及环境变量模板中存在形如用户名和密码的数据库/Redis 示例值 | 改为显式占位符，避免被误用为生产凭据 |
| 管理员身份 | 文档、默认配置、测试及前端含个人邮箱硬编码 | 文档与默认值改为通用示例；前端入口和页面改用后端权限校验 |
| Docker 示例 | 注释中的 PostgreSQL 服务有弱默认密码回退 | 改为必须显式提供本地环境变量 |
| 密码验证 | 开发模式原先跳过密码校验，任何输入都可通过已注册账户登录 | 移除跳过逻辑，并加入错误密码回归测试 |
| 登录日志 | 登录流程原先会记录用户邮箱 | 改为不含邮箱的状态日志 |

## 后续注意

1. 现有 Git 历史中的旧示例凭据和旧硬编码邮箱不会因当前文件修改而消失。本次**没有重写历史、强制推送或删除本机数据**。若旧示例值曾在实际服务中使用，应立即轮换；如需彻底清理公开历史，需另行评估并获得授权。
2. `.env`、数据库及日志虽然被 Git 忽略，但仍可被手工压缩、复制或截图带出。分享项目时只提供脱敏模板和本报告，不包含这些本机文件。
3. 本次是静态检查，不能证明所有外部副本、远端分支或历史附件都没有泄露，也不验证密钥是否仍有效。
4. 管理员页面现在依赖后端校验。需要管理员功能时，在本机私有配置中单独设置管理员身份；不要把真实邮箱写回代码。

## 验证

- Git 跟踪/忽略状态和本地提交已检查。
- 后端完整测试：161 项通过；前端 TypeScript 类型检查通过；`git diff --check` 通过。
