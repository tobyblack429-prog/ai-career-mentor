# 打开职业导师网站

双击本文件夹内的 `start.bat`。脚本会检查并启动前端和后端，确认数据库与网页就绪后，自动打开浏览器。

网站地址：http://localhost:3000/dashboard

启动完成后可以关闭启动窗口。两个服务在后台运行；重复双击会复用已就绪的服务。

## 自动启动与恢复

运行本文件夹的 `enable-auto-start.ps1` 后，会安装 Windows 计划任务 `AI Career Mentor - Local Recovery`：登录 Windows 时启动后台守护程序，每次检查后间隔 30 秒再次检查；服务退出后自动尝试重新启动。计划任务每分钟确认守护程序在运行，电脑从睡眠中恢复后也会继续检查。后台检查不会自动弹出浏览器，平时直接打开上面的网址即可。

安装配置由本文件夹的 `enable-auto-start.ps1` 完成；更换电脑或移动项目后需重新核对配置。这个机制运行在当前 Windows 用户下，不保存密码。

重启后请先登录 Windows，并给服务一点启动时间。如果仍打不开，双击 `start.bat` 查看具体提示。故障恢复针对进程退出；端口被其他程序占用、运行环境被删除或数据库故障时，需要按日志处理。

要停止自动检查，可在 Windows“任务计划程序”中先禁用再结束 `AI Career Mentor - Local Recovery`。这不会删除项目；结束该任务也可能停止由其启动的网站服务，此后仍可用 `start.bat` 手动启动。

如果启动失败，请查看窗口提示及 `logs` 文件夹中对应时间的日志；`logs/last-health-check.json` 记录最近一次自动检查结果。脚本不会终止占用端口的其他程序。

供排查使用：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\start-local.ps1 -NoBrowser` 只启动和检查服务，不打开浏览器。
